let seconds = 0
let chronoIntervalId;//ce chronoIntervalId et =0 mais de cette maniere elle est forcement indefine si on ne lui inisie aucune valeur 

//les constante ici  


//fonction setinterval permet de demarer avec un +1

//pour le stratseconde on dit que tout les seconde du chronoseconde sont egale a 0

//a voir le setInterval 

//a voir le clearInterval

//a voir le indefind structure de controle
// pour les valeur a l'interieur d'un test return true 

//A voir updateElements(element, text) fonction qui permet la mise a jour de l'element 